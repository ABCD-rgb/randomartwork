# Random Artwork  

A React JS project that displays a random artwork with a click of a button.   
  
*The artworks are retrieved from [Art Institute of Chicago API](http://api.artic.edu/docs/#introduction).*

## How to Run
1. Open terminal
2. Run `npm install` to install needed dependencies
3. Run `npm start` to start the app.
